---
# file: sssd-testing.yml
# Playbook for installing sssd and configuration files for auth specific to GCB at Duke
- hosts:
    - test
  vars_files:
    - "vars/{{ ansible_os_family }}.yml"
  roles:
    - auth

# vim: set sw=2 ts=2:
