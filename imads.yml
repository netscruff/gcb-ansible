- name: Deploy webserver for viewing predictions inside Docker
  hosts: imads
  become: yes
  roles:
    - { role: angstwad.docker_ubuntu, when: "ansible_os_family == 'Debian'" }
    - docker_webapp
    - imads_webapp
